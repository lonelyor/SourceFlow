# SourceFlow 当前任务队列

更新日期：2026-05-22

## 本轮

- [ ] 易用性增强阶段一：文档树导航增强，先完成快速过滤、当前路径条、最近编辑/常用入口和状态标识。
- [ ] 阶段一验证：评审 diff，运行 TypeScript、SCSS、语言 JSON 与必要后端测试。
- [ ] 易用性增强阶段二：正文定位增强，完成 Sticky 当前标题、大纲与正文双向高亮、标题折叠体验增强。
- [ ] 阶段二验证：评审 diff，运行 TypeScript、SCSS 和编辑器相关验证。
- [ ] 易用性增强阶段三：整理提示与块级链接增强，完成空文档/重名提示和块级定位链接复制增强；孤立文档暂不处理。
- [ ] 阶段三验证：评审 diff，运行 TypeScript、SCSS、语言 JSON 与必要后端测试。
- [x] 文档树数量显示 — 先更新 plans 文档，再实现配置、样式与验证。
- [x] 新增子文档数量开关，默认关闭，行尾弱化显示非零子文档数。
- [x] 新增全部笔记总数开关，默认开启，标题栏右侧轻量显示已打开笔记本下的文档总数。
- [x] 验证 TypeScript 编译、SCSS 编译、语言 JSON 和 Go 测试。
- [x] 编辑器结构提示 — 先更新 plans 文档，再实现配置、样式与验证。
- [x] 新增标题层级常驻标识，默认开启，显示 H1-H6。
- [x] 新增顶层块编号显示开关，默认关闭，不实现真实视觉换行号。
- [x] 验证 TypeScript 编译、SCSS 编译、语言 JSON 和 Go 默认值测试。
- [x] 文档树外观增强 — 先更新 plans 文档并提交，再修改配置和样式。
- [x] 新增外观配置：层级引导线开关默认关，文档树密度默认保持当前。
- [x] 增强拖拽落点提示、当前路径/选中/拖拽目标区分和可展开等待反馈。
- [x] 验证 TypeScript 编译、SCSS 编译、语言 JSON 和 Go 目标测试，不改变拖拽移动与排序语义。
- [x] 修复既有 Go vet 日志格式串阻断，确保 `go test ./api -count=1` 通过。
- [x] 文档树拖拽移动增强 — 先更新 plans 文档，明确移动与排序分阶段拆分。
- [x] 阶段一：增强移动落点，支持拖到目标文档、笔记本根、展开目录空白区和子列表末尾，并保持任意排序模式下可移动。
- [x] 阶段一：普通文档树拖拽只走移动，不自动切换自定义排序，不调用排序接口。
- [x] 阶段一：验证 TypeScript 编译；记录 `typecheck:app` 受既有 wysiwyg 模块守卫影响。
- [x] 阶段二：新增自定义排序右键菜单“向上移 / 向下移”，避免普通拖拽移动承担排序语义。
- [x] 更新 v0.1.3 版本号、changelog 和 AI 助手相关文档。
- [x] 运行 `python 编译.py` 完成本机编译与产物校验。
- [x] 审查 `git diff`，仅提交本轮授权文档、版本和 AI 助手相关变更。
- [x] 用户已确认按当前整个工作区快照继续发布。
- [x] 审查 `git diff`，提交当前整个工作区快照对应变更。
- [x] 运行 `python 发布.py` 发布 v0.1.3。
- [x] 工具拒绝按钮 — 前端渲染 + 事件 + API + 后端拒绝逻辑。
- [x] Gemini 原生 function calling（streaming + non-streaming）。
- [x] 补充前端行为测试覆盖（工具确认/拒绝、Enter 发送、Escape 取消编辑、编辑消息 action）。

## 后续

- [ ] 发布后 24 小时内按 `docs/OPERATIONS.md` 检查启动、便携包、插件集市、同步诊断和崩溃日志。
- [ ] 考虑将图片附件、编辑重发链路的前端行为测试也逐步补齐。
