# 编辑器跨块复制粘贴安全强化设计

日期：2026-06-05

## 状态

- 阶段 1 已落地：新增 selection scope 判定、跨块文本替换事务、内部剪贴板 MIME 收紧和回归测试。
- 该实现改变复制/粘贴边界判定，属于高影响编辑器行为调整。
- 目标是让复制粘贴保持无感、直接、符合直觉，同时消除误伤其他块的风险。

## 问题结论

用户反馈：在某个块中复制粘贴大段内容时，如果内容本身不换行，有概率复制到其他块；手动换行后粘贴则没有问题。

代码审计确认存在风险路径：

- 普通正文块通过 CSS 软换行显示长文本，`word-break: break-word` 与 `white-space: break-spaces` 只影响视觉排版，不形成真实块边界。
- 普通文本选择如果跨过多个块，当前复制链路仍可能把包含 `[data-node-id]` 的片段写入 SourceFlow 内部剪贴板 MIME。
- 粘贴时内部 MIME 会被当作 Block DOM 处理，可能从“我只想复制文本”变成“复制了块结构”。
- `insertHTML()` 在 inline 粘贴路径中直接执行 `range.deleteContents()`，但事务只记录起始块 old/new HTML；如果 range 实际跨块，可能先修改多个块 DOM，再只提交单块事务。

这类问题的核心不是 Markdown 解析，也不是后端数据层，而是前端编辑器 selection、剪贴板 MIME 和事务边界不一致。

## 设计目标

1. 单块文本操作只能修改单块。
2. 块级复制粘贴只能由明确块选择触发。
3. 普通文本选择即使跨块，也不能伪装成内部 Block DOM 粘贴。
4. 跨块选择不得使用单块 `update` 事务包装。
5. 复制粘贴是高频操作，用户不应在常规 Ctrl+C / Ctrl+V 中看到失败提示或被要求理解块边界。
6. 无法证明结构安全时，程序必须自动降级为纯文本流或安全块事务，而不是打断用户。
7. 所有变更必须保持撤销/重做原子、可审计、可回滚。
8. 不通过自动插入换行、截断文本或改变正文 CSS 来规避问题，避免改变用户笔记内容语义。

## 非目标

- 不改变用户笔记中的真实换行、空格、Markdown 语义。
- 不改变显式块选择、块菜单复制、块拖拽和块移动语义。
- 不重写整个 Protyle 粘贴系统。
- 不引入新的配置项让用户承担安全边界选择。

## 人类直觉语义

### 复制

- 光标在块内且没有选区：复制当前块，沿用现有块复制语义。
- 显式块选择：复制块，写入 SourceFlow 内部 Block DOM MIME。
- 单块文本选区：复制文本和安全的行内格式；如果片段不包含 `[data-node-id]`，可以写入行内 SourceFlow MIME。
- 跨块文本选区：复制用户选中的文本内容和标准 HTML 展示片段，但不写入 SourceFlow 内部 Block DOM MIME。
- 表格/数据库视图专用选择：继续走现有表格/AV 专用路径。

### 粘贴

- 光标处粘贴普通文本：插入到当前块或按 Markdown 自然解析。
- 单块文本选区上粘贴：只替换当前块内选区。
- 显式块选择上粘贴块：按块级替换/插入语义处理。
- 普通跨块文本选区上粘贴：自动按文本流替换选区；如果粘贴内容可安全解析为块，则生成覆盖所有受影响块的块级事务；如果不能证明块结构安全，则降级为纯文本流事务。
- 内部 Block DOM 粘贴：只接受来自显式块选择或可信块复制的内部 MIME。

## 核心不变量

### Selection Scope

新增统一选择范围判定模块，建议路径：

- `app/src/protyle/util/selectionScope.ts`

核心类型：

```ts
type TSelectionScopeKind =
    | "collapsed"
    | "single-block-text"
    | "multi-block-text"
    | "explicit-block"
    | "table"
    | "attribute-view"
    | "code"
    | "unsupported";
```

核心 API：

```ts
interface ISelectionScope {
    kind: TSelectionScopeKind;
    startBlock?: HTMLElement;
    endBlock?: HTMLElement;
    blockIds: string[];
    crossesBlock: boolean;
    containsBlockDOM: boolean;
    isExplicitBlockSelection: boolean;
}

resolveSelectionScope(range: Range, wysiwygElement: HTMLElement): ISelectionScope
assertInlineRangeWithinSingleBlock(scope: ISelectionScope): boolean
canWriteInternalSourceFlowClipboard(scope: ISelectionScope, fragment: DocumentFragment): boolean
```

判定规则：

- `collapsed`：无选中文本，且 range 在编辑器内。
- `single-block-text`：start/end 都落在同一个 `[data-node-id]` 内，且不是显式块选择。
- `multi-block-text`：start/end 落在不同 `[data-node-id]` 内，且不是显式块选择。
- `explicit-block`：存在 `.protyle-wysiwyg--select` 或明确块菜单/块选择来源。
- `table` / `attribute-view`：保持专用选择路径优先级。
- `unsupported`：不得抛给用户处理；必须归一化为最保守的安全操作，优先纯文本流粘贴，无法确定落点时退回当前起始块内插入。

## 复制强化

修改位置：

- `app/src/protyle/wysiwyg/commonEvents/copy.ts`

设计：

1. 复制入口先调用 `resolveSelectionScope()`。
2. 只有以下场景允许写入 `Constants.SOURCEFLOW_HTML_CLIPBOARD_MIME`：
   - `explicit-block`
   - 当前已有块菜单或 gutter 块复制路径
   - `single-block-text` 且复制片段不包含任何 `[data-node-id]`，只包含行内 span、link、ref、mark、emoji 等安全行内节点
3. `multi-block-text` 不写内部 MIME，只写：
   - `text/plain`：按选区顺序输出文本，块边界归一化为单个换行
   - `text/html`：标准 HTML 展示片段，移除 `data-node-id`、`updated`、`contenteditable`、`spellcheck` 等块运行时属性
4. 如果 range.cloneContents() 中出现 `[data-node-id]`，但 scope 不是 `explicit-block`，必须降级为普通文本/标准 HTML。

这样用户从一个长段里拖选文本时，即使浏览器 range 包含了相邻块结构，也不会在粘贴时被当作块复制。

## 粘贴强化

修改位置：

- `app/src/protyle/util/paste.ts`
- `app/src/protyle/util/insertHTML.ts`
- `app/src/protyle/util/multiBlockPaste.ts`

设计：

1. `paste()` 读取内部 MIME 后先校验来源和目标 scope，生成粘贴计划，不直接执行 DOM 删除。
2. 如果剪贴板含 SourceFlow 内部 MIME，但目标是普通文本粘贴，必须判断它是否来自可信块选择：
   - 可信块 MIME + 显式块目标：走块级事务。
   - 非可信块 MIME 或普通文本选区：丢弃内部 MIME，使用 `text/plain` 或清洗后的标准 HTML 生成文本流事务。
3. `insertHTML()` 在任何 `range.deleteContents()` 前必须做 scope 守卫：
   - `collapsed`：按当前逻辑插入，但内部 MIME 仍需经过可信来源判断。
   - `single-block-text`：只允许单块 inline 替换。
   - `multi-block-text`：禁止走单块 update；改走多块文本替换事务。
   - `explicit-block`：走块级事务路径，不走单块 update。
4. 所有安全降级都必须在 DOM 修改前完成，用户不需要确认，也不应该看到失败提示。
5. 多块文本替换必须新增独立事务构造器，不能复用单块 `insertHTML()` 的直接 DOM 删除路径。

## 多块文本替换方案

第一阶段就必须支持普通跨块文本选区粘贴替换，不能把失败提示暴露给用户。

新增专用事务：

- 对 startBlock、endBlock 和中间块分别生成 oldHTML 快照。
- 先在 detached DOM 中模拟替换，生成每个受影响块的 newHTML。
- 事务必须包含所有受影响块的 `update/delete/insert` 操作。
- 如果粘贴内容可安全解析为块 DOM，按块结构生成事务：
  - 起始块保留选区前缀并拼接粘贴内容第一块。
  - 结束块保留选区后缀并拼接粘贴内容最后一块。
  - 中间完全被选中的块删除。
  - 粘贴内容中间块按顺序插入。
- 如果粘贴内容无法安全解析为块 DOM，降级为纯文本流：
  - 把选区跨越的块边界归一化为换行。
  - 把粘贴文本按换行生成段落事务。
  - 不保留未知或不可信的运行时块属性。
- 如果选区包含表格、数据库视图、代码块、HTML 块、资源块等复杂结构：
  - 若命中该结构的专用 CV 路径，走专用路径。
  - 否则把复杂结构作为原子边界处理，不做半结构删除。
  - 粘贴落点自动归一到起始文本块或该复杂结构之后，避免误删复杂块。
- 不允许直接对真实 DOM 执行 `range.deleteContents()` 后再推导事务。

## 无提示策略

复制粘贴是高频基础能力，常规 Ctrl+C / Ctrl+V 不应出现失败提示、确认框或安全术语。

处理策略：

- 能精确保留结构时，自动走结构化事务。
- 不能证明结构安全时，自动走纯文本流事务。
- 无法确认复杂结构的半选区语义时，保护复杂结构本身，把粘贴内容放入最接近用户光标的安全文本落点。
- 只在系统异常、事务提交失败、剪贴板读取权限失败等非预期错误时使用现有错误提示；普通跨块选择不是错误。

## 测试设计

新增脚本建议：

- `app/scripts/testProtylePasteSelectionSafety.js`

覆盖点：

1. 单块文本选区允许 inline 粘贴。
2. 普通跨块文本选区不允许写入 SourceFlow 内部 Block DOM MIME。
3. 普通跨块文本选区会生成多块文本替换事务，不进入单块 `insertHTML()` 删除路径。
4. 显式块选择仍允许写入内部 Block DOM MIME。
5. 片段包含 `[data-node-id]` 但不是显式块选择时，复制降级为普通 HTML/text。
6. block-ref/link/mark 等纯行内片段仍能保留行内格式。
7. 表格、AV、代码块路径不被新 guard 误拦截。
8. 跨块选择粘贴单行长文本时，不弹提示、不误删相邻块，并保持一次撤销可还原。
9. 跨块选择粘贴多行文本时，按自然段落生成事务，并保持一次撤销可还原。

验证命令：

```bash
pnpm --dir app run test:protyle-paste-selection-safety
pnpm --dir app run test:protyle-blockdom-preservation
pnpm --dir app run typecheck:app
pnpm --dir app run test:editor-structure-guide
pnpm --dir app run test:editor-structure-guide-bugfix
git diff --check
```

如果项目脚本还没有 `test:protyle-paste-selection-safety`，需要在 `app/package.json` 增加脚本，并接入 `typecheck:app` 的结构检查链路。

## 分阶段落地

### 阶段 1：无感安全 CV

- 新增 selection scope helper。
- 复制时限制内部 MIME 写入条件。
- 粘贴时禁止普通跨块文本选区进入单块 inline 删除路径，改为多块文本替换事务或纯文本流事务。
- 补静态/DOM 回归测试。

阶段 1 的用户体验目标：用户继续直接 Ctrl+C / Ctrl+V，不需要理解块边界；程序自行选择文本流、块级事务或复杂结构保护策略。

落地状态：已完成。当前多块替换优先覆盖同父级普通段落/标题；遇到列表跨层级、表格、数据库视图、代码块、HTML 块、资源块等复杂结构时，先折叠到起始安全落点再走普通插入，保护复杂结构不被半删除。

### 阶段 2：复杂结构精细化

- 扩展复杂结构半选区处理能力，例如跨普通段落和列表项、引用块、callout 的混合选择。
- 尽量保留更多行内格式和块语义。
- 所有受影响块仍必须纳入同一个事务。
- 保留阶段 1 的纯文本流降级兜底。

阶段 2 复杂度更高，建议在阶段 1 稳定后独立推进。

## 设计评审

结论：设计通过，可以作为后续实现依据。

关键理由：

- 不改变笔记正文语义，不靠自动换行规避问题。
- 将文本选择和块选择严格分离，符合块编辑器用户直觉。
- 第一阶段不再失败关闭，而是通过自动归一化和纯文本流降级保证 CV 高频体验。
- 多块替换有明确事务模型，不复用当前单块 `range.deleteContents()` 路径。
- 用户不需要在复制粘贴时理解块边界；程序必须自行选择最安全、最接近直觉的执行方式。
